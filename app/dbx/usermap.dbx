// dbx.v1 golang usermap.dbx .

model gerrit_user (
    key gerrit_username

    field gerrit_username text
    field chat_id         text
)

read one (
    select gerrit_user.chat_id
    where  gerrit_user.gerrit_username = ?
)

create gerrit_user ( noreturn )
